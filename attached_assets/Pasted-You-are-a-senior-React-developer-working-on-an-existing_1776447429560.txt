You are a senior React developer working on an existing production project called "Sohelix".

The project already contains 100+ tools and uses:

* React (Vite)
* Client-side rendering (createRoot, NO hydration)
* Static pre-rendering (SSG) for SEO
* Tailwind UI system
* Existing export features (Copy, Print, PDF, CSV)
* Existing chart system and summary calculations

🚨 CRITICAL CONSTRAINTS (VERY IMPORTANT):

* DO NOT change UI design, layout, spacing, or styling
* DO NOT modify routing or URLs
* DO NOT break any existing features (chart, summary, export, SEO)
* DO NOT change component structure unnecessarily
* DO NOT refactor unrelated code
* Only replace the input logic safely

GOAL:
Update the "Currency Denomination Calculator" by replacing the "Bundles (×100)" input system with a **single input: "No. of Notes/Coins"**, while keeping ALL other features exactly the same.

---

CORE CHANGE (STRICT):

❌ REMOVE ONLY:

* Bundles (×100) input field
* Any ×100 multiplication logic related to bundles

✅ REPLACE WITH:

* A single input field:
  "No. of Notes/Coins"

---

CALCULATION UPDATE:

Previously:
subtotal = (bundles × 100 + loose) × denomination

Now:
subtotal = count × denomination

---

WHAT MUST REMAIN EXACTLY THE SAME:

1. UI Layout:

* Table design
* Input styling
* Spacing and alignment
* Summary card UI
* Chart UI
* Buttons (Copy, Reset, Print, PDF, CSV)

2. FEATURES:

* Live calculation on input change
* Summary:

  * Total Amount
  * Total Notes
  * Total Coins
* Chart distribution
* Export functionality (Copy, PDF, CSV, Print)
* Reset button
* Currency selector

3. DATA FLOW:

* Existing calculation system should continue working
* Only replace the source input (count instead of bundles)

---

LABEL UPDATE:

* Replace column label:
  "Bundles (×100)" → "No. of Notes/Coins"

---

LOGIC RULES:

* Default empty input = 0
* Prevent negative values
* Handle large numbers safely
* Notes = denominations ≥ ₹10
* Coins = denominations < ₹10

---

CHART + EXPORT SAFETY:

* Chart must still use subtotal values
* Exported data must reflect updated logic (count-based)
* Do not rewrite export system—only adapt input data

---

SEO SAFETY:

* Ensure totals and visible data remain in prerendered HTML
* Do not introduce client-only rendering for important values

---

CLEANUP:

* Remove all bundle-related variables and calculations
* Keep code minimal and clean
* Avoid unnecessary re-renders

---

FINAL OUTPUT:

* Show only necessary code changes (minimal diff)
* Clearly explain:

  * what was removed
  * what was replaced
* Confirm that no other feature was modified

---

EXPECTED RESULT:

* Same UI and features
* Only input method changed
* Simpler user experience
* No bugs, no console errors
* Fully compatible with existing system

Do NOT redesign. Do NOT over-engineer. Only perform a safe logic replacement.
